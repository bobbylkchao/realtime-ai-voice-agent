/**
 * Default HTTP base for Amazon Connect + OpenAI SIP webhooks (`{base}/incoming-call`).
 * Override with `AMAZON_CONNECT_PHONE_WEBHOOK_BASE_PATH`.
 */
export const AMAZON_CONNECT_PHONE_WEBHOOK_BASE_PATH_DEFAULT =
  '/amazon-connect-phone'
